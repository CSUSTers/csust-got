name: Pull Request

on:
  pull_request:

jobs:

  lint:
    name: Lint
    uses: ./.github/workflows/lint.yml


  test:
    name: Test
    uses: ./.github/workflows/test.yml


  go-build:
    name: Go Build
    uses: ./.github/workflows/go-build.yml
